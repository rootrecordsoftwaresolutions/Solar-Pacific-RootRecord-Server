# poll buckets — STUB ONLY

Folders: `1m/` `5m/` `15m/` `30m/` `hour/` `daily/`

**Do not enable aggressive polling yet.** Build after atomic actions under `scripts/actions/` are proven.
Samples destination when enabled: `/home/rootrecord/Database/ENERGY/buckets/`.
