# ==============================================================================
# council relay runtime (key=value) — edit offline like jobs.py
# Old council group (NOT data relay) from ava-council state.json
# ==============================================================================
ENABLED=1
COUNCIL_CHAT_ID=-1004367256267
POLL_TIMEOUT=20
MAX_TEXT=3900
# Long-poll uses AVA token only (one getUpdates). Replies send with each voice token.
POLL_VOICE=ava
RUN_OLLAMA=/home/rootrecord/.ollama/skills/plumbing/scripts/run-ollama.sh
DESK_LIVE_FILE=
STATE_DIR=/home/rootrecord/Database/intake/council-relay
LOG_DIR=/home/rootrecord/Database/GITHUB/logs
SECRETS_1=/home/rootrecord/.config/ava-council/secrets.env
SECRETS_2=/home/rootrecord/master/master-key.env
# Comma-separated substrings (lowercase) that trigger FULL pipeline AVA→Bruce→Carly→Ava
# Without these: single voice only (mentioned bot, or DEFAULT_SINGLE_VOICE)
PIPELINE_TRIGGERS=team all,team_all,a>b>c,pipeline,full council,council round,guys discuss,everyone weigh in,all three
DEFAULT_SINGLE_VOICE=ava
RUN_INFER=/home/rootrecord/.ollama/skills/plumbing/scripts/run-infer.sh
