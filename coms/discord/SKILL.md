# Discord Skill (coms/discord) — not yet built

No implementation exists yet. Placeholder folder so `coms/` has its final
shape. When built, this should follow the same pattern as `coms/ssh` and
`coms/telegram`: its own `SKILL.md`, `scripts/`, and (if needed)
`references/` — loaded independently so using SSH or Telegram never pulls
Discord's instructions into context.
