# Separate repositories
aws-sync/

# Archives and backups
*.zip
backups/

# Media Artifacts
*.wav
*.mp3

# Agent packets: allow thin SKILL.md + docs (working code).
# Heavy context zips stay out via global *.zip rule.
# Do NOT commit secrets into agents/*/ 
# /agents/  (removed 2026-09-22 — Ava Ivy: track SKILL.md for GitHub identity pushes)

# Environment / secret files
.env
*.env


# --- github / poller runtime (never auto-commit) ---
github/scripts/poll-and-push.log
logs/store/rootserver-poller.log
automations/scripts/__pycache__/
**/__pycache__/
*.pyc

# dated change backups — live under /home/rootrecord/Database/GITHUB
automations.bak-*/
*.bak-*/

# website Next build artifacts
website/site/node_modules/
website/site/.next/
us-mainland-server/
aws-sync.retired*/
plumbing/state/
