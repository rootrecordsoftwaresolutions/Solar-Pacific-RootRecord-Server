# RootRecord status — 2026-09-22 (HST)

Authoritative “what is live **now**” after OmniBook rebuild day. Supersedes older session blockers where they conflict.

---

## 1. OmniBook live stack

### Rootserver poller + Cloudflare
- Public: `https://rootserver.rootrecord.cloud/`
- Code: `~/.ollama/skills/automations/` (`rootserver_poller.py`, `jobs.py`, `poller-watch.py`)
- Shortcut: `/home/rootrecord/rootserver-poller` (`restart|stop|start|window|status`)
- **MUST:** Ctrl-C / stop kills poller + cloudflared + unit (not window-only)
- Local HTTP heartbeat: `http://127.0.0.1:8799/`

### `jobs.py` boot order (priority)
0. self / status terminal  
1. Cloudflare tunnel  
2. GitHub `setup-all-remotes`  
3. `ollama_warmup`  
4. `flm_npu_warmup` (FastFlowLM)  
5. `council_relay` ensure (skips if legacy `apps.council` still running)

### Recurring
- Heartbeat every 5s  
- `github_sync_all` every 300s → `skills` + `website` + `mainland`

---

## 2. Data layout
| Path | Purpose |
|------|---------|
| `/home/rootrecord/Database/intake/` | Data we take in |
| `/home/rootrecord/Database/GITHUB/` | Baks, logs, cutover scripts, worktrees |
| `/home/rootrecord/Database/RootRecord Context/` | Ideology / architecture docs (also mirrored under this handoff) |
| `/home/rootrecord/Database/DAILY AI DEV HANDOFF/` | **This pack** — zip for free AIs |

Never dump baks into `~/.ollama/skills/` (floods git autopush).

---

## 3. GitHub (three repos)
Configured in `~/.ollama/skills/github/scripts/repos.conf`:

| id | Mode | Local | GitHub |
|----|------|-------|--------|
| skills | inplace | `~/.ollama/skills` | Solar-Pacific-RootRecord-Server |
| website | mirror | `skills/website/site` | RootRecord-Website |
| mainland | inplace | `skills/us-mainland-server` | US-Mainland-Server |

Pacific `.gitignore` excludes: `website/`, `us-mainland-server/`, `aws-sync/`, `plumbing/state/`, …  
Sync uses `git add -A` each cycle (new files/folders auto-staged unless ignored).  
**Note:** `~/.ollama/agents` (Modelfiles) is **outside** skills — not in Solar-Pacific push.

`aws-sync` / rclone retired (GitHub push/pull model). AWS 1‑min pull timer may still need install on the mainland host.

---

## 4. Agents — Ollama + FLM (NPU)

### Ollama (CPU fallback) — `dolphin-mistral:latest` lanes
Desk: `~/.ollama/agents/` · registry `lanes.conf` · `create-all.sh`  
Models: `ava` / `ava-public` / `ava-architect` / `ava-telegram` · `bruce` / `bruce-ops` / `bruce-philosophy` / `bruce-telegram` · `carly` / `carly-appsec` / `carly-energy` / `carly-telegram` · `vision-reader` (staged)

**Honesty:** without measured desk → say cannot see desk / No data. Standing envelopes ≠ live readings.

### FastFlowLM (NPU) — preferred infer
- Binary/service pattern: `flm serve … --host 127.0.0.1 --port 52625`
- Default chat model in plumbing: **`llama3.2:3b`** (many other models also listed on the FLM server)
- Proven chat endpoint: `POST /v1/chat/completions`
- Entry: `~/.ollama/skills/plumbing/scripts/run-infer.sh`  
  → FLM if up, else Ollama `*-telegram`  
  → always via `single-flight.sh` (one run at a time)

### Plumbing
`~/.ollama/skills/plumbing/` — NPU-first policy docs, single-flight lock, warmup scripts, pipelines (vision NPU still staged).

---

## 5. Telegram council (rebuilt)

**Not** the Data Relay (`@rootreceiver_bot` / `@rootsender_bot`).  
**Council group** chat id (from `~/.config/ava-council/state.json`): `-1004367256267`

Live path: `~/.ollama/skills/coms/telegram/`
- `config/voices.conf` — ava/bruce/carly → token env + Ollama model names  
- `config/bots.conf` — bot usernames registry  
- `config/relay.conf` — `COUNCIL_CHAT_ID`, pipeline triggers, `RUN_INFER`  
- `scripts/council-relay.py` — **one** getUpdates (poll token = Ava)  
- Routing:
  - `@one` / one name → **that bot only** (posts with **that** token)
  - Pipeline phrases (`team all`, `a>b>c`, `pipeline`, …) → **A→B→C→A**, each hop own bot token
  - Operator silence cues (`do not say anything`, `nowhere near ready`) → **no post**
  - Replies must not leak DESK_LIVE / HARD RULES (sanitized / dropped)

Tokens present as env names: `TELEGRAM_AVA_TOKEN`, `TELEGRAM_BRUCE_TOKEN`, `TELEGRAM_CARLY_TOKEN` (never print values).

Grok Bot lane (Ava/Bruce/Carly panel agents) stays **manual** — no auto Telegram→Grok.

---

## 6. Coms layout
Parent: `~/.ollama/skills/coms/`  
Children: `telegram/`, `discord/`, `slack/`, `ssh/` (+ staged `council/` notes).  
SSH skill = data-pack / AWS path (distinct from council).

---

## 7. Website
Skill desk: `~/.ollama/skills/website/` (Next.js foundation).  
GitHub: RootRecord-Website (mirror sync). Foundation only — status page No data/Waiting style.

---

## 8. Explicitly NOT done / Waiting
- Live EcoFlow / host-metrics wired into `DESK_LIVE_FILE` for infer
- AWS host systemd `aws-git-pull.timer` (1 min) — prepare locally; install on mainland when ready
- Vision NPU EP (staged docs only)
- Multi-bot DM long-poll (group @ works; DM poll is Ava token only to avoid 409)
- Full legacy `apps.council` UX (brainstorm/desks) re-port — new relay is the live council path for now
- Re-port of pre-reset skills from `Old-online-safe` zip (historical only unless operator names a module)

---

## 9. Quick operator commands
```bash
/home/rootrecord/rootserver-poller status|restart|window
bash ~/.ollama/skills/plumbing/scripts/run-infer.sh bruce 'one sentence status'
bash ~/.ollama/skills/coms/telegram/scripts/status.sh
pgrep -af 'council-relay|flm serve|rootserver_poller|apps.council'
bash ~/.ollama/skills/github/scripts/sync-all.sh
```

---

## 10. Grok panel agents (context synced 2026-09-22)
Profiles updated to mention Ollama lanes, plumbing single-flight, DESK_LIVE honesty, staged/live coms telegram relay, NPU-prefer FLM. Ava + Carly confirmed memory store; Bruce Monitor (this lane) profile updated.
