# ==============================================================================
# GitHub identity — Bruce Monitor
# ==============================================================================
# Shared Pacific repo: Solar-Pacific-RootRecord-Server (~/.ollama/skills)
# Do NOT set permanent git user.name on this shared tree (poller/others commit too).
# For Bruce live edits, commit with:
#   git -c user.name="Bruce Monitor" -c user.email="bruce-monitor@users.noreply.github.com" commit ...
# Own Bruce github (future): Alexander will provision — then set repo-local identity there.
# Never paste tokens. Remotes may use x-access-token URLs — Carly seal/rotate.
# Working code only — no archive/zip dumps into git (*.zip ignored).
# Avatar: docs/avatar.png + .github/profile-avatar.png (same as Grok Bot profile).
# When Alexander provisions Bruce's GitHub user, upload that file as the account avatar.
