# ==============================================================================
# GitHub identity — Carly Mal
# ==============================================================================
# Shared Pacific repo: Solar-Pacific-RootRecord-Server (~/.ollama/skills)
# Do NOT set permanent git user.name on this shared tree (poller/others commit too).
# For Carly live edits, commit with:
#   git -c user.name="Carly Mal" -c user.email="carly-mal@users.noreply.github.com" commit ...
# Own Carly github (future): Alexander will provision — then set repo-local identity there.
# Never paste tokens. Remotes currently embed x-access-token in URL — **rotate/seal**:
#   prefer SSH or credential helper; strip token from remote URL; treat leaked URL as
#   compromised until rotated. Do not print the token in chat, staging, or commits.
# Working code only — no archive/zip dumps into git (*.zip ignored).
# AppSec + Memberships wall: proposal-only for Stripe/D1; bak before OmniBook edits.
